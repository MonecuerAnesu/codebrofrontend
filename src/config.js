// src/config.js
const BASE_URL = "https://codebro-1.onrender.com";
export default BASE_URL;
// This is the base URL for the backend API, which can be used throughout the frontend application
// to make requests to the server. It is set to the deployed URL on Render.